# Core dependencies for Door Controller
# Raspberry Pi GPIO control
# sudo apt install python3-rpi-lgpio
# from RPi import GPIO
# Windows-friendly emulator (used automatically on Windows)

# Google Sheets integration
# Note: imports are now lazy in start.py so missing packages don't crash startup
gspread==6.2.1
# oauth2client is deprecated but functional through Python 3.13.
# pkg_resources usage is from setuptools; no known breaking change in 3.13.
oauth2client==4.1.3

# Adafruit PN532 NFC/RFID reader
adafruit-circuitpython-pn532==2.4.7
adafruit-blinka==8.47.0

# Required by Adafruit libraries (transitive dependencies)
adafruit-circuitpython-busdevice==5.2.15
Adafruit-PlatformDetect==3.87.0
Adafruit-PureIO==1.1.11
pyftdi==0.57.1

# Google API dependencies
google-auth==2.49.2
google-auth-oauthlib==1.2.1
httplib2==0.31.2

# Standard library enhancements (Python compatibility)
typing-extensions==4.15.0
# required for google auth
cryptography>=38.0.0
# required for ssl generation better to do locally and copy cert
cryptography>=38.0.0


