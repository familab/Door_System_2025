# Windows development requirements (minimal)
# Exclude hardware-specific packages which may fail to build on Windows
gspread==6.2.1
oauth2client==4.1.3
google-auth-oauthlib==1.3.1
unittest-xml-reporting>=3.0.0
typing-extensions==4.15.0
cryptography>=38.0.0
# Optional emulator package to provide RPi.GPIO compatibility
RPi.GPIO-emulator
